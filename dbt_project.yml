name: 'analytics_engineer_assessment'
version: '1.0.0'
config-version: 2

profile: 'analytics_engineer_assessment'

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
data-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  analytics_engineer_assessment:
    staging:
      materialized: view
      +schema: staging
    intermediate:
      materialized: view
      +schema: intermediate
    marts:
      materialized: table
      +schema: marts
